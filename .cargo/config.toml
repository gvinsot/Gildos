# Build configuration for the Gildos workspace.
#
# We deliberately do NOT pin a default `target` here so the host toolchain
# (x86_64 Linux, aarch64 Linux, aarch64 macOS) can build natively. CI
# (see CONTRIBUTING.md §8) walks the §17 four-tier matrix explicitly.

[net]
git-fetch-with-cli = true
